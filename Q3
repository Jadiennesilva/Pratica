#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
	char frase[700];
	
	printf("Qual frase você deseja: ");
	gets(frase);
	
	printf("qual foi a frase lida ao contrário: \n");
	for (int idx = strlen(frase); idx >= 0; idx--){
		printf("%c", frase[idx]);
	}
	
	return 0;
}
